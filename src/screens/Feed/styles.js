import {StyleSheet} from 'react-native';
import {COLOR_LIGHT} from '../../styles/colors';
import {Fontsize, MARGIN, normalized, PADDING} from '../../styles/maxing';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
