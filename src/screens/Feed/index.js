import {connect} from 'react-redux';
import Feed from './container';
const mapStateToProps = (state) => ({});

const mapDispatchTopProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchTopProps)(Feed);
